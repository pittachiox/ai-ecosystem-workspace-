# API Package

The API package contains versioned FastAPI route modules and controller logic.

## Responsibilities
- Endpoint grouping by domain
- Request validation and response formatting
- Controller-to-service interaction
- Docs metadata and versioned route organization
